(function() {
    'use strict';

    var relatorioIndCtrl = function($scope) {
    };

    relatorioIndCtrl.$inject = ['$scope'];

    angular.module('controleApp')
        .controller('relatorioIndCtrl', relatorioIndCtrl);

})();
