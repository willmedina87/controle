(function() {
    'use strict';

    var relatorioGeralCtrl = function($scope) {
    };

    relatorioGeralCtrl.$inject = ['$scope'];

    angular.module('controleApp')
        .controller('relatorioGeralCtrl', relatorioGeralCtrl);

})();
